services:
  - type: web
    name: deploy-go
    env: go
    plan: free
    buildCommand: go mod download && go build -o main .
    startCommand: ./main
    healthCheckPath: /health
    envVars:
      - key: GIN_MODE
        value: release
